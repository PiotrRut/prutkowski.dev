import styled from 'styled-components';

export const PicButton = styled.button`
  background: none;
  padding: 0;
  border: 0;
  box-shadow: none;
  &:hover {
    cursor: pointer;
  }
  &:focus {
    outline: none;
  }
`;
