const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  theme: {
    container: {
      center: true,
    },
    colors: {
      black: colors.black,
      gray: colors.gray,
      blue: colors.blue,
      green: colors.green,
    },
  },
};
